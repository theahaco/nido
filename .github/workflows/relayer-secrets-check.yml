# One-shot wiring check for the relayer deploy pipeline (#72):
# 1Password service account -> nido_fly_io vault -> Fly API token -> flyctl auth.
# Public repo: never print secret values or account identities; statuses only.
# Subsumed by deploy-relayer.yml once the real deploy workflow lands.
name: relayer-secrets-check

on:
  push:
    branches: [feat/72-relayer-gas-abstraction]
    paths: ['.github/workflows/relayer-secrets-check.yml']
  workflow_dispatch:

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Install 1Password CLI
        run: |
          set -euo pipefail
          curl -sSfo /tmp/op.zip "https://cache.agilebits.com/dist/1P/op2/pkg/v2.34.0/op_linux_amd64_v2.34.0.zip"
          unzip -oq /tmp/op.zip -d /tmp/opcli
          sudo mv /tmp/opcli/op /usr/local/bin/op
          op --version

      - name: Locate Fly token in 1Password
        env:
          OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}
        run: |
          set -euo pipefail
          if [ -z "${OP_SERVICE_ACCOUNT_TOKEN}" ]; then
            echo "::error::OP_SERVICE_ACCOUNT_TOKEN secret is empty or unavailable"
            exit 1
          fi

          # Known from the 2026-06-09 discovery run: vault "theahaco", item "nido_fly_io"
          VAULT="theahaco"
          ITEM="nido_fly_io"

          if ! ITEM_JSON=$(op item get "$ITEM" --vault "$VAULT" --reveal --format json 2>/tmp/op-err); then
            if ! ITEM_JSON=$(op item get "$ITEM" --vault "$VAULT" --format json 2>>/tmp/op-err); then
              echo "::error::op item get '$ITEM' failed:"
              cat /tmp/op-err
              exit 1
            fi
          fi

          # Diagnosis without values: field labels/ids/types and whether each has a value
          echo "fields on op://$VAULT/$ITEM (label | id | type | has_value):"
          echo "$ITEM_JSON" | jq -r '.fields[]? | "\(.label // "-") | \(.id) | \(.type) | \(.value != null and .value != "")"'

          FIELD=$(echo "$ITEM_JSON" | jq -c '[.fields[]? | select(.value != null and .value != "") | select(.id == "credential" or .purpose == "PASSWORD" or ((.label // "") | ascii_downcase | test("token|credential|password|secret|api|key")))][0] // empty')
          if [ -z "$FIELD" ]; then
            FIELD=$(echo "$ITEM_JSON" | jq -c '[.fields[]? | select(.value != null and .value != "") | select(.type == "CONCEALED")][0] // empty')
          fi
          if [ -z "$FIELD" ]; then
            echo "::error::no field with a value found on op://$VAULT/$ITEM (see field table above)"
            exit 1
          fi
          TOKEN=$(echo "$FIELD" | jq -r '.value')
          LABEL=$(echo "$FIELD" | jq -r '.label // .id')
          REF="op://$VAULT/$ITEM/$LABEL"

          while IFS= read -r line; do
            [ -n "$line" ] && echo "::add-mask::$line"
          done <<< "$TOKEN"

          echo "found credential field -> secret reference for deploy workflow: $REF"
          {
            echo "FLY_API_TOKEN<<G2C_EOF_TOKEN"
            echo "$TOKEN"
            echo "G2C_EOF_TOKEN"
          } >> "$GITHUB_ENV"

      - name: Setup flyctl
        uses: superfly/flyctl-actions/setup-flyctl@master

      - name: Verify Fly token against Fly API
        run: |
          set -euo pipefail
          if OUT=$(flyctl auth whoami 2>&1); then
            echo "flyctl auth whoami: OK (identity withheld from public log)"
          elif OUT=$(flyctl apps list 2>&1); then
            echo "flyctl auth whoami unavailable (likely a scoped deploy token), but flyctl apps list: OK"
          else
            echo "::error::Fly token failed to authenticate"
            echo "$OUT" | tail -3
            exit 1
          fi
          echo "--- orgs visible to this token ---"
          flyctl orgs list 2>&1 || echo "(orgs list not permitted for this token scope — fine for app-scoped deploy tokens)"
